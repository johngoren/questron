// Given an array of choice numbers (0-2), calculate which category the user falls into.
//
// Depending on how we implement the game.json, this might, instead of receiving an array of choice numbers, receive an array of "this score" or "that score" numbers.


// const SCORE_CATEGORY_1 = 0;
// const SCORE_CATEGORY_2 = 1;
// const SCORE_CATEGORY_3 = 2;


export default function calculateScoreFromAnswers(answers) {
    
}